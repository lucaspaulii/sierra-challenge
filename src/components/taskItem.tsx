import { Task } from "~/utils/hooks/useTasks";

export default function TaskItem({
  task,
  handleClick,
  handleEdit,
}: {
  task: Task;
  handleClick: Function;
  handleEdit: Function;
}) {
  return <div></div>;
}
